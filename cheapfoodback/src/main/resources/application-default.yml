base:
  url: http://localhost:8080
spring:
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  data:
    jpa:
      repositories:
        enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/fooddb2
    username: rft
    password: test123
    driver-class-name: org.postgresql.Driver
    platform: postgres
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
    show-sql: true
  level:
    root: info